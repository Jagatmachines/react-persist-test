// import ThunkAction from 'redux-thunk';

const addTodoItem = () => (
    {
        type: 'addTodo',
        payload: 'data ayo hai'
        /* return (dispatch) => {
            let dispatchtype = 'addTodo';
            let payloadData = 'data added vayo'
            return dispatch({
                
              })
        } */
    })

export default addTodoItem;